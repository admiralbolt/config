" Enable modern Vim features not campatible with Vi spec.
set nocompatible

" Share yank / paste with the system clipboard.
set clipboard=unnamedplus

" Show line numbers by default.
set number

" Tab should be 2 spaces, as God intended.
set tabstop=2 softtabstop=0 expandtab shiftwidth=2 smarttab
